syntax = "proto3";
package tinyim;

import "access/access.proto";

option cc_generic_services = true;


service DbproxyService {
    rpc SaveMsg(Msg) returns (Reply);
    rpc GetMsg() returns (Msg);
}